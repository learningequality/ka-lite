<div class="form-group">
    <label class="sr-only" for="group-name">Group</label>
    <select class="form-control" id="group-name">
        {{#if groups }}
            {{#each groups}}
                <option data-group-id="{{id}}">{{ name }}</option>
            {{/each}}
            <option>All</option>
        {{else}}
            <option disabled>All</option>
        {{/if}}
    </select>
</div>