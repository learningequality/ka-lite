<div class="row">
	<div class="col-md-12">
		{{#if students}}
			<div id="displaygrid">
				<div class="users-table">
					<table class="table table-striped table-bordered">
						<tbody>
							<tr>
								<th class="headrowuser">
									gettext{"text here"}
								</th>
							</tr>
							for (var i = 0; i < students.length; i++) {
								students = students[i];
								<tr>
									<th class="username">
										<span title="{{ student.name }} ({{ student.username }})">
											<div class="student-name">
												<a href="{% url 'student_view' %}?user={{ student.id }}">{{ student.name }}</a>
											</div>
										</span>
									</th>
								</tr>
							} <!-- end for -->
						</tbody>
					</table>
					<div style="clear: both;">
					</div>
				</div>
			</div>
            if (request_report_type == "exercise" && exercises) {

            } <!-- endif -->

            else if (request_report_type == "video" && videos) {

            } <! -- end else if -->
        {{/if}} 
	</div>
</div>
<!-- end block report content-->