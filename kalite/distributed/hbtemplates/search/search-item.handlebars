<span class='autocomplete {{#unless available }}un{{/unless}}available'>
    <span class = 'dropdown-icon icon-{{ kind }}'>&nbsp;</span>
    <span>{{ title }}</span>
    <span>({{ kind }})</span>
</span>&nbsp;